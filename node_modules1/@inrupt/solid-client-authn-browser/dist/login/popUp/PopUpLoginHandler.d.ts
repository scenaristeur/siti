import { ILoginOptions, ILoginHandler, ISessionInfoManager, LoginResult } from "@inrupt/solid-client-authn-core";
export default class PopUpLoginHandler implements ILoginHandler {
    private loginHandler;
    private sessionCreator;
    constructor(loginHandler: ILoginHandler, sessionCreator: ISessionInfoManager);
    canHandle(loginOptions: ILoginOptions): Promise<boolean>;
    handle(loginOptions: ILoginOptions): Promise<LoginResult>;
}
